'use strict';


// Declare app level module which depends on filters, and services
angular.module('token', [
  'ngRoute',
  'token.filters',
  'token.services',
  'token.directives',
  'token.controllers'
]);
